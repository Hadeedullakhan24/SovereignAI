import type {ButtonHTMLAttributes,ReactNode} from 'react'; export const Button=({children,...p}:ButtonHTMLAttributes<HTMLButtonElement>)=><button className="button" {...p}>{children}</button>; export const Card=({children}:{children:ReactNode})=><section className="card">{children}</section>; export const Empty=({children}:{children:ReactNode})=><div className="empty">{children}</div>; export const ErrorView=({error,retry}:{error:unknown;retry?:()=>void})=><div className="error">{error instanceof Error?error.message:typeof error==='object'&&error&&'message'in error?String(error.message):'Unable to load this data.'}{retry&&<Button onClick={retry}>Try again</Button>}</div>; export const Badge=({state}:{state:string})=><span className={'badge '+state.toLowerCase()}>{state.replaceAll('_',' ')}</span>; export const fmt=(v?:string)=>v?new Intl.DateTimeFormat(undefined,{dateStyle:'medium',timeStyle:'short'}).format(new Date(v)):'—';
