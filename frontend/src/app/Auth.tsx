import {createContext,useContext,useEffect,useState,type ReactNode} from 'react'; import {authApi} from '../services/api'; import {tokenStore} from '../services/api/client';
type Auth={ready:boolean;authenticated:boolean;login:(email:string,password:string)=>Promise<void>;register:(email:string,password:string)=>Promise<void>;logout:()=>Promise<void>}; const C=createContext<Auth|null>(null);
export function AuthProvider({children}:{children:ReactNode}){const [ready,setReady]=useState(false),[authenticated,setAuth]=useState(false);useEffect(()=>{setAuth(!!tokenStore.get());setReady(true)},[]);const login=async(e:string,p:string)=>{await authApi.login(e,p);setAuth(true)},register=async(e:string,p:string)=>{await authApi.register(e,p);await login(e,p)},logout=async()=>{await authApi.logout();setAuth(false)};return <C.Provider value={{ready,authenticated,login,register,logout}}>{children}</C.Provider>}; export const useAuth=()=>{const v=useContext(C);if(!v)throw Error('AuthProvider missing');return v};
